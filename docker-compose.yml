# EnerjiOne Grid Dashboard — tek-komut Docker Compose dosyasi.
#
# Kurulum:
#   1. cp .env.example .env
#   2. .env'deki SECRET_KEY, INTERNAL_SERVICE_TOKEN, POSTGRES_PASSWORD,
#      RABBITMQ_PASSWORD degerlerini DEGISTIR (`openssl rand -hex 32`).
#   3. docker compose up -d --build
#   4. Browser: http://<vds-ip>/  (port 80)
#
# Ilk acilista backend-api `Base.metadata.create_all` ile semayi olusturur.
# Sonra installer hesabi olusturmak icin:
#   docker compose exec backend-api python scripts/create_installer.py \
#       --username admin --password 'StrongPassw0rd!' --full-name "Sistem Admin"
#
# (scripts/create_installer.py mevcutsa; degilse README'de manuel SQL adimi.)

services:
  postgres:
    image: postgres:16-alpine
    container_name: e1-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-enerjione}
      POSTGRES_USER: ${POSTGRES_USER:-enerjione}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD .env'de olmali}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-enerjione} -d ${POSTGRES_DB:-enerjione}"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - e1-net

  rabbitmq:
    image: rabbitmq:3.13-management-alpine
    container_name: e1-rabbitmq
    restart: unless-stopped
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER:-hsl}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASSWORD:?RABBITMQ_PASSWORD .env'de olmali}
    volumes:
      - rabbitmq-data:/var/lib/rabbitmq
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 15s
      timeout: 10s
      retries: 10
    ports:
      # AMQP — uzaktaki gateway'lerin (saha kurulumlari, baska VDS, ofis vs.)
      # bagh kabilmesi icin internet'e acik. Guvenlik: dedicated user/parola
      # ve "loopback_users" disable degil; backend her gateway icin ayri user
      # uretiyor (gw-<code>). Default 'hsl' admin kullanicisi sadece backend
      # tarafindan kullanilir, parolasi .env'de.
      - "5672:5672"
      # Management UI host'a sadece localhost'tan acilir; genel internete kapali.
      # Tunelle gormek icin: ssh -L 15672:localhost:15672 user@vds
      - "127.0.0.1:15672:15672"
    networks:
      - e1-net

  # NATS JetStream — yuksek hacimli telemetri akisi icin RabbitMQ'nun yaninda
  # calisir. Baslangicta dual-publish modu: gateway hem RabbitMQ'ya hem JetStream'e
  # yazar; tuketiciler hangisinden okuyacagini feature flag ile secer.
  # NATS_DUAL_PUBLISH_ENABLED=false oldugu surece bu servis bos durur ve mevcut
  # akisi etkilemez. Cutover sonrasi alarm/bildirim RabbitMQ'da kalmaya devam eder.
  nats:
    image: nats:2.10-alpine
    container_name: e1-nats
    restart: unless-stopped
    command:
      - "-js"
      - "--store_dir=/data"
      - "--http_port=8222"
      - "--name=e1-nats-1"
    volumes:
      - nats-data:/data
    healthcheck:
      test: ["CMD", "wget", "-q", "-O-", "http://localhost:8222/healthz"]
      interval: 15s
      timeout: 5s
      retries: 10
    ports:
      # NATS client port — uzaktaki gateway'ler (leaf node veya dogrudan) buraya
      # baglanir. Guvenlik: cluster icinde TLS+auth eklenmeli (ileride).
      - "4222:4222"
      # HTTP monitoring (varz/connz/jsz) — sadece localhost'tan.
      - "127.0.0.1:8222:8222"
    networks:
      - e1-net

  backend-api:
    build:
      context: ./apps/backend-api
      dockerfile: Dockerfile
    image: e1/backend-api:latest
    container_name: e1-backend-api
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        # Alarm akisi (alarm.created) RabbitMQ uzerinden gectigi icin
        # rabbitmq saglikli olmali; telemetri akisi NATS'tan gelir.
        condition: service_healthy
      nats:
        # Telemetri akisinin tek yolu; backend startup'inda stream'leri ensure
        # eder. NATS gelmeden persister consumer baslayamaz (yine de backend
        # ayagi kalir, NATS gelince consumer reconnect dongusunde baglanir).
        condition: service_healthy
    environment:
      APP_ENV: production
      SECRET_KEY: ${SECRET_KEY:?SECRET_KEY .env'de olmali}
      DATABASE_URL: postgresql+psycopg2://${POSTGRES_USER:-enerjione}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-enerjione}
      CORS_ORIGINS: ${CORS_ORIGINS:-*}
      RABBITMQ_URL: amqp://${RABBITMQ_USER:-hsl}:${RABBITMQ_PASSWORD}@rabbitmq:5672/
      RABBITMQ_MANAGEMENT_URL: http://rabbitmq:15672
      RABBITMQ_ADMIN_USERNAME: ${RABBITMQ_USER:-hsl}
      RABBITMQ_ADMIN_PASSWORD: ${RABBITMQ_PASSWORD}
      # NATS JetStream — telemetri akisinin PRIMARY rotasi. Backend startup'ta
      # stream'leri otomatik ensure eder (TELEMETRY_RAW, TELEMETRY_NORMALIZED).
      NATS_URL: ${NATS_URL:-nats://nats:4222}
      INTERNAL_SERVICE_TOKEN: ${INTERNAL_SERVICE_TOKEN:?INTERNAL_SERVICE_TOKEN .env'de olmali}
      ACCESS_TOKEN_MINUTES: ${ACCESS_TOKEN_MINUTES:-43200}
      TELEMETRY_RETENTION_MINUTES: ${TELEMETRY_RETENTION_MINUTES:-30}
      TELEMETRY_RETENTION_INTERVAL_SEC: ${TELEMETRY_RETENTION_INTERVAL_SEC:-300}
      SMTP_ENABLED: ${SMTP_ENABLED:-false}
      SMTP_HOST: ${SMTP_HOST:-}
      SMTP_PORT: ${SMTP_PORT:-587}
      SMTP_USERNAME: ${SMTP_USERNAME:-}
      SMTP_PASSWORD: ${SMTP_PASSWORD:-}
      SMTP_FROM_EMAIL: ${SMTP_FROM_EMAIL:-noreply@enerjione.local}
      # DB yedek dosyalari icin sabit dizin. backup-data volume'una baglanir
      # ki container yeniden olusturulsa da yedekler kaybolmaz.
      BACKUP_DIR: /var/lib/e1-backups
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8000/api/v1/health"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 30s
    ports:
      # Uzaktaki gateway'ler dogrudan baglanabilsin diye 8000 host'a expose.
      # Browser yine de port 80 (nginx) uzerinden /api/* proxy'sini kullanir;
      # 8000 sadece gateway'in REST cagrilari icindir (internal service token
      # ile zaten korunmali; ek olarak CORS_ORIGINS uretimde kisitlanmali).
      - "8000:8000"
    volumes:
      - backup-data:/var/lib/e1-backups
    networks:
      - e1-net

  tag-engine:
    build:
      context: ./apps/tag-engine
      dockerfile: Dockerfile
    image: e1/tag-engine:latest
    container_name: e1-tag-engine
    restart: unless-stopped
    depends_on:
      nats:
        condition: service_healthy
    environment:
      # Tag-engine artik tamamen NATS JetStream uzerinde calisiyor.
      # In: TELEMETRY_RAW stream (hsl.telemetry.raw.>)
      # Out: hsl.telemetry.normalized.<gw> (TELEMETRY_NORMALIZED stream'e gider)
      NATS_URL: ${NATS_URL:-nats://nats:4222}
      # Backend system-status, tag-engine icin 8011 portunu probe eder.
      WORKER_HEALTH_HOST: 0.0.0.0
      WORKER_HEALTH_PORT: 8011
    networks:
      - e1-net

  alarm-service:
    build:
      context: ./apps/alarm-service
      dockerfile: Dockerfile
    image: e1/alarm-service:latest
    container_name: e1-alarm-service
    restart: unless-stopped
    depends_on:
      rabbitmq:
        condition: service_healthy
      backend-api:
        condition: service_healthy
      nats:
        condition: service_healthy
    environment:
      # Telemetri kaynagi: NATS JetStream (tag-engine cikisi).
      NATS_URL: ${NATS_URL:-nats://nats:4222}
      # Alarm yayini: RabbitMQ (notification-worker buradan tuketiyor).
      RABBITMQ_URL: amqp://${RABBITMQ_USER:-hsl}:${RABBITMQ_PASSWORD}@rabbitmq:5672/
      RABBITMQ_EXCHANGE: hsl.events
      RABBITMQ_DLX_EXCHANGE: hsl.events.dlx
      BACKEND_API_BASE: http://backend-api:8000/api/v1
      BACKEND_INTERNAL_ALARM_URL: http://backend-api:8000/api/v1/internal/alarms
      BACKEND_INTERNAL_ALARM_CLEAR_URL: http://backend-api:8000/api/v1/internal/alarms/clear
      INTERNAL_SERVICE_TOKEN: ${INTERNAL_SERVICE_TOKEN}
      WORKER_HEALTH_HOST: 0.0.0.0
      WORKER_HEALTH_PORT: 8012
    networks:
      - e1-net

  notification-worker:
    build:
      context: ./apps/notification-worker
      dockerfile: Dockerfile
    image: e1/notification-worker:latest
    container_name: e1-notification-worker
    restart: unless-stopped
    depends_on:
      rabbitmq:
        condition: service_healthy
    environment:
      RABBITMQ_URL: amqp://${RABBITMQ_USER:-hsl}:${RABBITMQ_PASSWORD}@rabbitmq:5672/
      RABBITMQ_EXCHANGE: hsl.events
      RABBITMQ_DLX_EXCHANGE: hsl.events.dlx
      BACKEND_API_BASE: http://backend-api:8000/api/v1
      INTERNAL_SERVICE_TOKEN: ${INTERNAL_SERVICE_TOKEN}
      WORKER_HEALTH_HOST: 0.0.0.0
      WORKER_HEALTH_PORT: 8013
    networks:
      - e1-net

  iec104-outbound:
    build:
      context: ./apps/iec104-outbound
      dockerfile: Dockerfile
    image: e1/iec104-outbound:latest
    container_name: e1-iec104-outbound
    restart: unless-stopped
    depends_on:
      backend-api:
        condition: service_healthy
      nats:
        condition: service_healthy
    environment:
      # Telemetri kaynagi: NATS JetStream (tag-engine cikisi).
      NATS_URL: ${NATS_URL:-nats://nats:4222}
      BACKEND_API_BASE: http://backend-api:8000/api/v1
      INTERNAL_SERVICE_TOKEN: ${INTERNAL_SERVICE_TOKEN}
      WORKER_HEALTH_HOST: 0.0.0.0
      WORKER_HEALTH_PORT: 8014
      IEC104_DEFAULT_LISTEN_HOST: 0.0.0.0
    # IEC 104 outbound master sistemler bu portlara baglanir.
    # Buradaki aralik (2404 standart, 2405-2410 ek hedefler) kullaniciya gore
    # genisletilebilir; backend'deki outbound_targets icindeki listen_port
    # bu araliga dusmeli.
    ports:
      - "2404:2404"
      - "2405:2405"
      - "2406:2406"
    networks:
      - e1-net

  frontend-web:
    build:
      context: ./apps/frontend-web
      dockerfile: Dockerfile
    image: e1/frontend-web:latest
    container_name: e1-frontend-web
    restart: unless-stopped
    depends_on:
      - backend-api
    ports:
      # VDS'in 80 portu — kullanici browser'i buraya baglanir; nginx hem SPA'i
      # serve eder hem /api/* isteklerini backend-api:8000'e proxy eder.
      - "${FRONTEND_HTTP_PORT:-80}:80"
    networks:
      - e1-net

networks:
  e1-net:
    driver: bridge

volumes:
  postgres-data:
  rabbitmq-data:
  nats-data:
  backup-data:

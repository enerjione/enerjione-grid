fastapi
uvicorn[standard]
sqlalchemy
psycopg2-binary
python-jose[cryptography]
passlib[bcrypt]
pydantic-settings
python-multipart
email-validator
bcrypt<5
pika
paho-mqtt
requests
openpyxl
# Sistem durumu sayfasinda CPU/RAM/disk/uptime metriklerini almak icin.
# psutil tum metrikleri standart API uzerinden saglar; manylinux + Windows
# wheel'leri PyPI'de hazir, ek native bagimliligi yok.
psutil>=5.9,<7
# Ariza email bildirimlerinde haritanin PNG goruntusunu uretmek icin.
# OSM tile'larini fetch edip uzerine polyline + marker cizer; Pillow
# bagimliligini kendi paketliyor.
staticmap>=0.5.6,<1
Pillow>=10.0,<11
# Mobil push bildirimleri icin (FCM HTTP v1 API).
# google-auth ile service account uzerinden OAuth2 token alip dogrudan
# REST'e POST atiyoruz; firebase-admin SDK'sinin tum stack'i (RTDB, Auth)
# yerine sadece push gerektigi icin daha hafif olsun.
google-auth>=2.0,<3
# NATS JetStream — telemetri akisinin RabbitMQ ile paralel yan yana
# calistirilabilmesi icin. Default'ta dual-publish KAPALI olsa da paket
# yine de container'a kurulur; runtime'da `nats_dual_publish_enabled`
# ve `nats_consume_enabled` flag'lerine gore aktive olur. Lazy import:
# paket yoksa bile backend ayaga kalkar (jetstream_bus.py warning log atar).
nats-py>=2.6,<3
